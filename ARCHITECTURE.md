# Visual Architecture Guide

## 🏗️ System Architecture - Both Versions

### Version 1: Local MySQL Setup

```
┌─────────────────────────────────────────┐
│         Your Laptop                     │
│                                         │
│  ┌───────────────┐   ┌──────────────┐  │
│  │ RentalClient  │──▶│ RentalServer │  │
│  │  (Console)    │   │   (TCP:5000) │  │
│  └───────────────┘   └───────┬──────┘  │
│                              │          │
│                              ▼          │
│                       ┌─────────────┐   │
│                       │ MySQL DB    │   │
│                       │ (localhost) │   │
│                       └─────────────┘   │
└─────────────────────────────────────────┘
```

**Characteristics:**
- ✅ Everything runs locally
- ✅ Works offline
- ❌ Not accessible from other machines
- ❌ Requires MySQL installation

---

### Version 2: Cloud Deployment (Supabase + Railway)

```
┌──────────────────────┐
│   Your Laptop        │
│                      │
│  ┌────────────────┐  │
│  │ RentalClient   │  │
│  │   (Console)    │  │
│  └────────┬───────┘  │
└───────────┼──────────┘
            │
            │ Internet
            │ TCP Socket
            ▼
┌───────────────────────────────────┐
│      Railway.app (Cloud)          │
│                                   │
│   ┌───────────────────────────┐   │
│   │    RentalServer           │   │
│   │    - ClientHandler        │   │
│   │    - DAO Layer            │   │
│   │    (your-app.railway.app) │   │
│   └─────────────┬─────────────┘   │
└─────────────────┼─────────────────┘
                  │
                  │ JDBC
                  │ (SSL encrypted)
                  ▼
┌───────────────────────────────────┐
│   Supabase (Cloud PostgreSQL)     │
│                                   │
│   ┌─────────────────────────┐     │
│   │  PostgreSQL Database    │     │
│   │  - users table          │     │
│   │  - vehicles table       │     │
│   │  - bookings table       │     │
│   └─────────────────────────┘     │
└───────────────────────────────────┘
```

**Characteristics:**
- ✅ Accessible from anywhere
- ✅ No local database needed
- ✅ Professional deployment
- ✅ Team collaboration ready
- ⚠️ Requires internet connection

---

## 🔄 Request Flow - Cloud Version

### User Login Flow

```
1. User Action
   │
   ▼
┌──────────────────┐
│ RentalClient     │  Input: email = "joyce@demo.com"
│ (Your Laptop)    │        password = "password"
└────────┬─────────┘
         │
         │ Send: "LOGIN|joyce@demo.com|password"
         ▼
┌──────────────────────────────┐
│ Railway Server               │
│  ┌────────────────────────┐  │
│  │ ClientHandler          │  │
│  │  - receives command    │  │
│  │  - parses parameters   │  │
│  └──────────┬─────────────┘  │
│             │                │
│             ▼                │
│  ┌────────────────────────┐  │
│  │ UserDAOImpl           │  │
│  │  login(email, pwd)    │  │
│  └──────────┬─────────────┘  │
└─────────────┼────────────────┘
              │
              │ JDBC Query:
              │ "SELECT * FROM users 
              │  WHERE email = ? AND password = ?"
              ▼
┌─────────────────────────────┐
│ Supabase PostgreSQL         │
│  ┌───────────────────────┐  │
│  │ users table          │  │
│  │ - id: 2              │  │
│  │ - name: "Joyce"      │  │
│  │ - email: "joyce@..." │  │
│  │ - password: "pass..."│  │
│  │ - role: "CUSTOMER"   │  │
│  └────────┬──────────────┘  │
└───────────┼─────────────────┘
            │
            │ Result: User object
            ▼
┌──────────────────────────────┐
│ Railway Server               │
│  ┌────────────────────────┐  │
│  │ ClientHandler          │  │
│  │  - formats response    │  │
│  └──────────┬─────────────┘  │
└─────────────┼────────────────┘
              │
              │ Send: "OK|LOGIN|Joyce|CUSTOMER"
              ▼
┌──────────────────┐
│ RentalClient     │  Display: "Login successful!"
│ (Your Laptop)    │          "Welcome, Joyce (CUSTOMER)!"
└──────────────────┘
```

---

## 🗄️ Database Structure - PostgreSQL (Supabase)

```
┌─────────────────────────────────────────────────────┐
│              Supabase PostgreSQL Database            │
└─────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────┐
│ TABLE: users                                 │
├────────┬──────────┬──────────┬──────────────┤
│ id     │ name     │ email    │ password     │ role
├────────┼──────────┼──────────┼──────────────┤
│ 1      │ Admin    │ admin@.. │ admin123     │ ADMIN
│ 2      │ Joyce    │ joyce@.. │ password     │ CUSTOMER
│ 3      │ John Doe │ john@..  │ pass123      │ CUSTOMER
└────────┴──────────┴──────────┴──────────────┴──────────┘

┌───────────────────────────────────────────────────────────────┐
│ TABLE: vehicles                                               │
├────┬──────┬────────┬────────┬────────────┬─────────┬─────────┤
│ id │ type │ brand  │ model  │ reg_number │ price   │ avail   │
├────┼──────┼────────┼────────┼────────────┼─────────┼─────────┤
│ 1  │ CAR  │ Toyota │ Innova │ KA01AB1234 │ 2000.00 │ TRUE    │
│ 2  │ CAR  │ Honda  │ City   │ KA02CD5678 │ 1500.00 │ TRUE    │
│ 3  │ CAR  │ Hyundai│ Creta  │ KA03EF9012 │ 1800.00 │ TRUE    │
│ 4  │ BIKE │ Yamaha │ FZ     │ KA02XY5678 │  700.00 │ TRUE    │
│ 5  │ BIKE │ Honda  │ CBR    │ KA04GH3456 │  900.00 │ TRUE    │
│ 6  │ BIKE │ RE     │ C350   │ KA05IJ7890 │  800.00 │ TRUE    │
└────┴──────┴────────┴────────┴────────────┴─────────┴─────────┘

┌──────────────────────────────────────────────────────────────┐
│ TABLE: bookings                                              │
├────┬─────────┬───────────┬────────────┬──────────┬──────────┤
│ id │ user_id │vehicle_id │ start_date │ end_date │ status   │
├────┼─────────┼───────────┼────────────┼──────────┼──────────┤
│    │         │           │            │          │          │
│    │    (No bookings yet - ready for implementation)        │
└────┴─────────┴───────────┴────────────┴──────────┴──────────┘

Foreign Keys:
  bookings.user_id    ──▶ users.id
  bookings.vehicle_id ──▶ vehicles.id
```

---

## 🌐 Network Communication

### Protocol: Text-based with Pipe Delimiters

```
┌───────────────────────────────────────────────────────────┐
│ Client → Server Commands                                  │
└───────────────────────────────────────────────────────────┘

REGISTER|name|email|password
    ↓
    Example: REGISTER|Alice Smith|alice@test.com|pass123


LOGIN|email|password
    ↓
    Example: LOGIN|joyce@demo.com|password


LIST_VEHICLES|type
    ↓
    Example: LIST_VEHICLES|CAR
    Example: LIST_VEHICLES|BIKE


LOGOUT
    ↓
    Example: LOGOUT


┌───────────────────────────────────────────────────────────┐
│ Server → Client Responses                                 │
└───────────────────────────────────────────────────────────┘

Success:
    OK|COMMAND|data1|data2|...
    ↓
    Example: OK|LOGIN|Joyce|CUSTOMER
    Example: OK|REGISTER|3
    Example: OK|LIST_VEHICLES|2|1,Toyota,Innova,...|2,Honda,City,...

Error:
    ERROR|message
    ↓
    Example: ERROR|Login failed. Invalid email or password.
    Example: ERROR|Not logged in
```

---

## 🔐 Environment Variables Flow

### Local Development

```
1. Developer Sets Variables
   ↓
   PowerShell:
   $env:DB_URL = "jdbc:postgresql://..."
   $env:DB_USER = "postgres.abc123"
   $env:DB_PASSWORD = "secret"
   
2. Java Reads Variables
   ↓
   DBUtil.java:
   String url = System.getenv("DB_URL");
   
3. Creates Connection
   ↓
   DriverManager.getConnection(url, user, password)
   
4. Connects to Supabase
   ↓
   [Your Laptop] ──▶ [Supabase PostgreSQL]
```

### Railway Deployment

```
1. You Set Variables in Railway Dashboard
   ↓
   Railway UI:
   DB_URL = jdbc:postgresql://...
   DB_USER = postgres.abc123
   DB_PASSWORD = secret
   
2. Railway Injects Variables at Runtime
   ↓
   Environment variables available to Java process
   
3. Java Reads Variables (same code!)
   ↓
   DBUtil.java:
   String url = System.getenv("DB_URL");
   
4. Creates Connection
   ↓
   DriverManager.getConnection(url, user, password)
   
5. Connects to Supabase
   ↓
   [Railway Container] ──▶ [Supabase PostgreSQL]
```

**Key Insight**: Same code works both locally and on Railway!

---

## 🚀 Deployment Process

### Step-by-Step Visual

```
┌─────────────────────┐
│ 1. Write Code       │  You: Write Java code
│    (Your Laptop)    │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ 2. Push to GitHub   │  git push origin main
│                     │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ 3. Railway Detects  │  Webhook triggers build
│    New Commit       │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ 4. Railway Builds   │  mvn clean package
│    with Maven       │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ 5. Railway Runs     │  java -jar app.jar
│    Your Server      │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ 6. Public URL       │  your-app.railway.app
│    Generated        │
└─────────────────────┘

Now accessible from anywhere! 🌍
```

---

## 📊 Component Interaction Matrix

```
┌────────────────────────────────────────────────────────────────┐
│                     Who Talks to Whom?                         │
└────────────────────────────────────────────────────────────────┘

Component         │ Talks To              │ Protocol
──────────────────┼───────────────────────┼─────────────────────
RentalClient      │ RentalServer          │ TCP Socket (text)
RentalServer      │ ClientHandler         │ Java method calls
ClientHandler     │ UserDAO / VehicleDAO  │ Java method calls
UserDAOImpl       │ Database (Supabase)   │ JDBC (PostgreSQL)
VehicleDAOImpl    │ Database (Supabase)   │ JDBC (PostgreSQL)
Railway           │ GitHub                │ Webhook
Railway           │ Supabase              │ JDBC over Internet
Your Laptop       │ Railway               │ TCP over Internet
Your Laptop       │ Supabase              │ JDBC (when local dev)


┌────────────────────────────────────────────────────────────────┐
│                     What Runs Where?                           │
└────────────────────────────────────────────────────────────────┘

Your Laptop:
  • RentalClient.java (always)
  • RentalServer.java (optional, for local dev)

Railway (Cloud):
  • RentalServer.java (production)
  • ClientHandler.java
  • All DAO classes
  • Model classes

Supabase (Cloud):
  • PostgreSQL database
  • Tables: users, vehicles, bookings

GitHub:
  • Source code repository
  • Version control
```

---

## 🎯 Three Deployment Scenarios

### Scenario A: Fully Local (Original)

```
[Your Laptop]
    ├─ RentalClient ──┐
    ├─ RentalServer ──┼─▶ MySQL
    └─ MySQL DB ──────┘

✓ No internet needed
✗ Not accessible remotely
```

### Scenario B: Hybrid (Local Server + Cloud DB)

```
[Your Laptop]
    ├─ RentalClient ──┐
    └─ RentalServer ──┼───[Internet]───▶ [Supabase]

✓ No local database
✓ Same data as production
✗ Server not accessible remotely
```

### Scenario C: Full Cloud (Production)

```
[Your Laptop]
    └─ RentalClient ──[Internet]──▶ [Railway]
                                       │
                                       ▼
                                   [Supabase]

✓ Accessible from anywhere
✓ Professional deployment
✓ Team collaboration
✓ No local DB or server needed
```

---

## 🔄 State Management

```
┌─────────────────────────────────────────┐
│ Client State (RentalClient.java)       │
├─────────────────────────────────────────┤
│ • loggedIn: boolean                     │
│ • (session managed per client instance) │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│ Server State (ClientHandler.java)      │
├─────────────────────────────────────────┤
│ • currentUser: User                     │
│ • (one instance per connected client)   │
│ • (thread-safe, isolated sessions)      │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│ Database State (Supabase)               │
├─────────────────────────────────────────┤
│ • Persistent storage                    │
│ • Users, Vehicles, Bookings             │
│ • Shared across all server instances    │
│ • Source of truth                       │
└─────────────────────────────────────────┘
```

---

**This completes the visual architecture guide!**

Use these diagrams to understand:
- How components interact
- Where each piece runs
- What deployment option fits your needs
- How data flows through the system
